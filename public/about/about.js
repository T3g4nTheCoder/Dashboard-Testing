const element = document.getElementById("1");

element.style.width = `${screen.availWidth}px`;
element.style.height = `${screen.availHeight/3}px`;
element.style.top = `${screen.availWidth/4}px`
element.style.left = `${screen.availWidth/2-15}px`
element.style.boxSizing = `${screen.availWidth}px`